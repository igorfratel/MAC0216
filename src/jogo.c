#include "arena.h"
int main() {
	cria_arena(15, 15);
	// INSTR *p = devolve_programa();
	// INSTR *p2 = devolve_programa();
	// insere_exercito(1, p);
	mostra_arena();
	printf("oi\n");
	// insere_exercito(1, p2);
	// escalonador(1);
	//
	// destroi_arena(minha_arena);


}
